# yaml-language-server: $schema=http://json-schema.org/draft-07/schema
description: Beacon response that includes record level details, grouped in Resultsets.
type: object
allOf:
  - $ref: "https://raw.githubusercontent.com/ga4gh-beacon/beacon-framework-v2-yaml/main/responses/beaconCoreResponse.yaml"
    description: 'Section of the Response that does not include record level details
    and just `boolean` or `count` responses.'
  - $ref: "https://raw.githubusercontent.com/ga4gh-beacon/beacon-framework-v2-yaml/main/responses/sections/beaconResultsets.yaml"
