# yaml-language-server: $schema=http://json-schema.org/draft-07/schema
description: Boolean (true/false) response section.
type: object
required:
  - exists
properties:
  exists:
    description: Indicator of whether any entry was observed. This should be non-null,
      unless there was an error, in which case an error response is expected instead
      of this one.
    $ref: "https://raw.githubusercontent.com/ga4gh-beacon/beacon-framework-v2-yaml/main/common/beaconCommonComponents.yaml#/definitions/Exists"