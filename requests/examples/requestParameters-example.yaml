# yaml-language-server: $schema=https://raw.githubusercontent.com/ga4gh-beacon/beacon-framework-v2/main/requests/requestParameters.json
exampleEntry:
  description: |
    All the required fields to query any kind of variant (e.g. SNP, DUP, etc.).
  type: object
  properties:
    id:
      description: Id of the entry to display.
      type: string